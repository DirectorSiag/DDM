workflow:
  rules:
    - if: '$CI_COMMIT_TAG =~ /-win$/ || $CI_COMMIT_TAG =~ /-linux$/'
      when: never
    - when: always

variables:
  GIT_CLEAN_FLAGS: -ffdx
stages:
  - build
  - release

build_linux:
  stage: build
  image: stateoftheartio/qt6:6.7-gcc-aqt
  tags:
    - ddm-linux-local
  only:
    - develop
    - tags
  script:
    - echo "Preparando entorno de Linux instalando dependencias gráficas..."
    - sudo apt-get update -qq && sudo apt-get install -y libgl1-mesa-dev libglu1-mesa-dev
    - echo "Compilando versión RELEASE en Debian..."
    - mkdir -p build_linux
    - cd build_linux
    - qmake ../DDM.pro CONFIG+=release
    - make -j$(nproc)
    - echo "Empaquetando librerías de Qt (deploy manual, equivalente a windeployqt)..."
    - mkdir -p ../deploy_linux/lib
    - cp DDM ../deploy_linux/
    - ldd DDM | grep "=> /" | awk '{print $3}' | grep -i qt | xargs -I{} cp -v {} ../deploy_linux/lib/
    - |
      cat > ../deploy_linux/run_ddm.sh << 'EOF'
      #!/bin/bash
      DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
      export LD_LIBRARY_PATH="$DIR/lib:$LD_LIBRARY_PATH"
      "$DIR/DDM" "$@"
      EOF
    - chmod +x ../deploy_linux/run_ddm.sh ../deploy_linux/DDM
  artifacts:
    name: "DDM-Linux-v$CI_COMMIT_TAG"
    paths:
      - deploy_linux/

build_windows:
  stage: build
  tags:
    - ddm-test-pc
  only:
    - tags
  script:
    - echo "Compilando versión RELEASE en Windows..."
    - $env:Path = "C:\Qt\6.7.3\mingw_64\bin;C:\Qt\Tools\mingw1120_64\bin;" + $env:Path
    - qmake DDM.pro CONFIG+=release
    - mingw32-make
    - windeployqt release\DDM.exe --compiler-runtime
    - echo "Armando carpeta de deploy limpia, sin archivos intermedios..."
    - New-Item -ItemType Directory -Force -Path deploy_windows | Out-Null
    - Copy-Item release\DDM.exe deploy_windows\
    - Copy-Item release\*.dll deploy_windows\ -ErrorAction SilentlyContinue
    - if (Test-Path release\platforms) { Copy-Item release\platforms deploy_windows\platforms -Recurse }
    - if (Test-Path release\styles) { Copy-Item release\styles deploy_windows\styles -Recurse }
    - if (Test-Path release\imageformats) { Copy-Item release\imageformats deploy_windows\imageformats -Recurse }
  artifacts:
    name: "DDM-Windows-v$CI_COMMIT_TAG"
    paths:
      - deploy_windows/

release_linux:
  stage: release
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  tags:
    - ddm-linux-local
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Generando Release para Linux directamente desde el comando..."
    - |
      release-cli create --name "DDM Linux $CI_COMMIT_TAG" \
        --description "Binarios oficiales para Linux Debian" \
        --tag-name "$CI_COMMIT_TAG-linux" \
        --assets-link "{\"name\":\"Descargar-DDM-Linux-Release\",\"url\":\"${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build_linux\"}"

release_windows:
  stage: release
  tags:
    - ddm-test-pc
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Generando Release para Windows usando Docker en PowerShell..."
    - |
      $TAG = $env:CI_COMMIT_TAG
      $SHA = $env:CI_COMMIT_SHA
      $URL = "$env:CI_PROJECT_URL/-/jobs/artifacts/$TAG/download?job=build_windows"
      $ASSET_JSON = '{"name":"Descargar-DDM-Windows-Release","url":"' + $URL + '"}'
      docker run --rm -e CI_JOB_TOKEN=$env:CI_JOB_TOKEN -e CI_SERVER_URL=$env:CI_SERVER_URL -e CI_PROJECT_ID=$env:CI_PROJECT_ID registry.gitlab.com/gitlab-org/release-cli:latest release-cli create --name "DDM Windows $TAG" --description "Ejecutables oficiales para Windows" --tag-name "$TAG-win" --ref "$SHA" --assets-link $ASSET_JSON